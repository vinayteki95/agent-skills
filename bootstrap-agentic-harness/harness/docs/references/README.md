# References

Dense, agent-scannable lookup facts extracted from external sources (platform
APIs, design tokens, third-party forms, protocol details). Master docs link
here instead of duplicating big tables.

**Pattern:** each reference is a flat set of facts/tables an agent can grep — not
prose. Keep them current when the underlying source changes (see
[`../DOC_MAINTENANCE.md`](../DOC_MAINTENANCE.md)).

<!-- FILL: add references as needed, e.g.:
| File | Purpose |
|------|---------|
| design-system-reference.md | color/type/spacing tokens |
| platform-capabilities.md | external API caveats + our usage |
-->
